name: Deploy Convertix Image Engine (Production)

on:
  push:
    branches: [ main ]
    paths:
      - services/image-engine/**

permissions:
  contents: read

concurrency:
  group: convertix-image-engine
  cancel-in-progress: true

env:
  IMAGE_NAME: convertix-image-engine
  ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}
  IMAGE_PORT: "7071"

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      # -----------------------------
      # 1. Checkout
      # -----------------------------
      - uses: actions/checkout@v4

      # -----------------------------
      # 2. Azure Login
      # -----------------------------
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
      - name: Set subscription
        run: |
          az account set --subscription "3b320191-f647-4cfa-8446-b08e3c87dc8e"

      - name: Debug ACR vars
        run: |
          echo "ACR_NAME=${{ secrets.ACR_NAME }}"
          echo "ACR_LOGIN_SERVER=${{ secrets.ACR_LOGIN_SERVER }}"

      # -----------------------------
      # 3. Build + Push Image (ACR build)
      # -----------------------------
      - name: Build and push image in ACR
        run: |
          az acr build \
            -r "${{ secrets.ACR_NAME }}" \
            -t "${{ env.IMAGE_NAME }}:latest" \
            -f services/image-engine/Dockerfile \
            .
      # -----------------------------
      # 6. Deploy to App Service
      # -----------------------------
      - name: Verify Image Engine App Service
        run: |
          az webapp show \
            --resource-group "${{ secrets.AZURE_RESOURCE_GROUP }}" \
            --name "${{ secrets.AZURE_IMAGE_ENGINE_APP_NAME }}"
      - name: Configure container settings
        run: |
          az webapp config container set \
            --resource-group "${{ secrets.AZURE_RESOURCE_GROUP }}" \
            --name "${{ secrets.AZURE_IMAGE_ENGINE_APP_NAME }}" \
            --container-image-name "$ACR_LOGIN_SERVER/${{ env.IMAGE_NAME }}:latest" \
            --container-registry-url "https://$ACR_LOGIN_SERVER" \
            --container-registry-user "${{ secrets.ACR_USERNAME }}" \
            --container-registry-password "${{ secrets.ACR_PASSWORD }}"

          az webapp config appsettings set \
            --resource-group "${{ secrets.AZURE_RESOURCE_GROUP }}" \
            --name "${{ secrets.AZURE_IMAGE_ENGINE_APP_NAME }}" \
            --settings PORT=${{ env.IMAGE_PORT }} WEBSITES_PORT=${{ env.IMAGE_PORT }}

          az webapp restart --resource-group "${{ secrets.AZURE_RESOURCE_GROUP }}" --name "${{ secrets.AZURE_IMAGE_ENGINE_APP_NAME }}"

      # -----------------------------
      # 7. Wait for health
      # -----------------------------
      - name: Health check
        run: |
          echo "⏳ Waiting for image engine..."
          for i in {1..24}; do
            if curl -fsS --max-time 6 "${{ secrets.IMAGE_ENGINE_BASE_URL }}/health" >/dev/null; then
              echo "✅ Image Engine is healthy"
              echo "🔎 Tools status:"
              curl -sS --max-time 10 "${{ secrets.IMAGE_ENGINE_BASE_URL }}/health/tools" || true
              exit 0
            fi
            echo "…still starting ($i)"
            sleep 10
          done
          echo "❌ Image Engine failed to start"
          echo "🔎 Last /health response:"
          curl -sS --max-time 10 "${{ secrets.IMAGE_ENGINE_BASE_URL }}/health" || true
          echo
          echo "🔎 Last /health/tools response:"
          curl -sS --max-time 10 "${{ secrets.IMAGE_ENGINE_BASE_URL }}/health/tools" || true
          exit 1
